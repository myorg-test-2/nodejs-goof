name: Polaris Scan Remote NewProj

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  polaris-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Black Duck Security Scan
        uses: blackduck-inc/black-duck-security-scan@v2
        with:
          polaris_server_url: ${{ vars.POLARIS_URL }}
          polaris_access_token: ${{ secrets.POLARIS_ACCESS_TOKEN }}
          polaris_application_name: ${{ github.repository_owner }}
          polaris_project_name: 'nodejs-goof-remote-test'
          polaris_branch_name: 'main'
          polaris_branch_parent_name: 'main'
          polaris_assessment_types: 'SAST'
          polaris_test_sast_type: 'SAST-FULL'
          polaris_test_sast_location: 'remote'
          github_token: ${{ secrets.GITHUB_TOKEN }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
